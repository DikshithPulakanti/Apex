[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "integration: needs live infra (Neo4j/Weaviate/Postgres/Redis) — run explicitly with `pytest -m integration`",
]
